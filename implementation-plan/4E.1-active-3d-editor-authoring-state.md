Drop this into Cursor:

```text
Phase 4E.1 — Active 3D Editor Authoring State

Status:
4E ✅ Runtime composition (structure → surfaces, shared deriveVisualState)
4E.1 🔄 Product-first editor — CONFIGURATION UI shipped; visual stack corrected
     Canonical: `4E.1-product-model-visual-stack.md`

Do not continue into 4F without approval.
Do not treat ChoiceValue as the source of all visual state.

==================================================
GOAL (CORRECTED)
==================================================

Visual stack:

ObjectAssetRevision → immutable raw baseline
ProductModel static setup → product normal look
Selection VisualBindings → configuration overrides
→ VisualState → Three.js

Invariant: ProductModel is renderable with zero Choices and zero VisualBindings.
Configuration modifies visual state; it does not construct it from nothing.

Editor split: MODEL vs CONFIGURATION (same scene/targets/ops; different intent).


Key shift:

CURRENT

Editor shell
→ hardcoded/static panels
→ partial preview plumbing
→ model mostly observed, not authored

TARGET

ProductRevision
+ ProductModel
+ Asset graph
+ VisualDocument
        ↓
Editor active state
        ↓
usercontent edits real targets/bindings/assets
        ↓
persist
        ↓
reload
        ↓
same deterministic scene

Invariant:

Editor state is an authoring projection of persisted CubeCom state,
not a second source of truth.

No Zustand dump. Save only domain authoring state.

==================================================
RELATION TO 4E
==================================================

4E = runtime composition checkpoint
  Selection → deriveVisualState → reconcileScene → history-independent scene

4E.1 = authoring client over that runtime
  load / select / author / dirty / save / reload → same scene

Reuse 4E reconcile + package deriveVisualState. Do not fork derivation.

==================================================
FIVE CAPABILITIES
==================================================

1. Real scene state

The editor loads the actual composed scene from the persisted ProductRevision.
No mock mappings, no demo objects, no disconnected local config as the
source of visual truth.

2. Real selection / target interaction

Clicking a mesh/object in the scene resolves to CubeCom identity:

- What Object3D did I click?
- Which ObjectAssetRevision / ObjectRuntimeInstance is it from?
- Which nodePath?
- Which ModelTarget, if any?
- Which material slots (if applicable)?
- Which VisualEffects / bindings currently affect it?

3. Real authoring state

Side panels edit persisted concepts, not editor-only placeholders.

Supported authoring (only):

- select mesh → create/edit ModelTarget
- select target → bind ChoiceValue
- binding operation → SET_MATERIAL | REPLACE_COMPONENT | SET_VISIBILITY

No 4F/4G concepts in the UI.

4. Dirty / save / reload

Distinguish:

persisted visual state
vs
unsaved authoring changes

Then:

edit → Save → API → reload → normalize → deriveVisualState → same scene

5. Scene ↔ authoring synchronization

UI and scene must agree on the same draft VisualDocument + Selection.

- Change Top Material = Walnut → scene updates
- Click table top → inspector shows real target / binding
- Change binding → preview updates from the same draft state

==================================================
CURRENT BASELINE (do not re-litigate)
==================================================

Already real enough to build on:

- ProductRevision bootstrap + GLB mount + hydrateVisualReplay
- normalizeVisualDocument / from-graph
- 4E replay (structure + root surfaces)
- Mappings panel: edit existing SET_MATERIAL / SET_VISIBILITY values + save
- persistVisualDocument (VisualEffect create/update/delete) + save-proof reload
- Preview panel drives Selection → replay

Still scaffold / incomplete:

- Scene outliner hardcoded chair tree
- Object / mapping / material inspectors largely fake
- No ModelTarget create from mesh click
- No create-new binding UX (only patch hydrated rows)
- REPLACE_COMPONENT read-only in mappings panel
- Materials / cameras / lights / environment / behaviors panels hardcoded
- scene-tree helpers exist but are not wired to pick/inspector

==================================================
IMPLEMENTATION SLICES (suggested order)
==================================================

A. Selection identity

Wire pointer pick → buildNodePath → match ModelTarget → list affecting bindings.
Show this in object inspector + outliner (live scene graph, not mock tree).
Carry ObjectRuntimeInstance / revision id when the pick is under a replacement.

B. ModelTarget authoring

From selection: create/edit ModelTarget (key, nodePath, targetType, materialSlot).
Persist via existing GraphQL. Reload VisualDocument after save.

C. Binding authoring

Create/delete VisualEffects for SET_MATERIAL | REPLACE_COMPONENT | SET_VISIBILITY
(choice value × target × value). Enforce 4E structural-surface conflict in UI
before save (API already rejects). Live draft replay on change.

D. Dirty / save / reload spine

Clarify dirty = unsaved domain authoring (targets + bindings), not gizmo noise.
Save only ModelTarget + VisualEffect (+ linked asset picks already on model).
Hard reload after save must equal fresh open of same revision.

E. Quarantine scaffold

Materials / cameras / lights / environment / behaviors panels: hide, disable,
or mark non-authoritative so they cannot look like persisted product truth.
Do not rebuild them in 4E.1.

==================================================
ACCEPTANCE
==================================================

Open real ProductRevision
→ exact composed 3D scene loads

Click real mesh
→ identify actual nodePath / ModelTarget (or “unbound”) / affecting bindings

Create or edit ModelTarget
→ save

Bind ChoiceValue to SET_MATERIAL | REPLACE_COMPONENT | SET_VISIBILITY
→ preview changes immediately from draft state

Save
→ hard reload

Same ProductRevision
→ same targets
→ same bindings
→ same scene

Also:

- No second deriveVisualState in the editor
- No Zustand dump as persistence
- Scaffold panels do not present fake authored state as product truth

==================================================
OUT OF SCOPE
==================================================

Do NOT implement:

- attachment points
- semantic transforms
- component-local materials
- decoration / engraving / decals
- shader authoring
- animation authoring
- generic asset library management
- customizer / storefront work
- new kernel concepts
- 4F / 4G UI

==================================================
DEFINITION OF DONE
==================================================

4E.1 is green when the Editor is a usable authoring client:

persisted CubeCom visual model
↔ selectable scene identity
↔ editable targets/bindings
↔ dirty/save/reload
↔ same deterministic composed scene

Then STOP before 4F.

Report:

- selection identity model
- ModelTarget / VisualEffect authoring flows
- dirty/save contract
- which panels became real vs quarantined
- tests + manual proof
- any contradiction with 4E runtime rules
```
